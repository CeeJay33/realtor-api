# Enable mod_rewrite for URL rewriting
RewriteEngine On

# Example rule to redirect all traffic to index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]
